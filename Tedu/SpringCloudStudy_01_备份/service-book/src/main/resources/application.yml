server:
  port: 8101
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///cloudstudy
    username: root
    password: root123
  application:
    name: bookservice  #应用名称
  cloud:
    nacos:
      discovery:
        # 配置nacos注册地址
        server-addr: localhost:8848